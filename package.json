{
  "private": true,
  "scripts": {
    "start": "parcel dev.html --no-cache --dist-dir dist --log-level info",
    "build": "parcel build index.html --dist-dir docs --log-level info --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^2.1.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.0"
  }
}
