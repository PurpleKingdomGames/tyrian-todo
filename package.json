{
  "private": true,
  "scripts": {
    "start": "parcel index.html --no-cache --dist-dir dist --log-level info",
    "build": "parcel build index.html --dist-dir docs --log-level info --no-source-maps --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.1.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.0"
  }
}
